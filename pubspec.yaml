name: expression_binary_tree
description: A command line app used to convert an expression into its postfix and prefix notations.
version: 1.0.0

environment:
  sdk: '>=2.8.1 <3.0.0'

#dependencies:
#  path: ^1.7.0

dev_dependencies:
  pedantic: ^1.9.0
